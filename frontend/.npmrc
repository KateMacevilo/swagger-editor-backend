# Project-level npm config: internal base images may set engine-strict=true globally,
# which aborts npm ci on transitive deps whose engines field is stricter than needed
# (e.g. swagger-client declares node>=22 while building on node:20 works fine).
engine-strict=false
